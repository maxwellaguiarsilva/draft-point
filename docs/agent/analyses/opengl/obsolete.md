# Obsolete

## Objective

`obsolete` is the superseded or redundant Core profile tier, kept only for backwards compatibility (e.g. legacy aliased constants).

## Nodes

- `superseded-alias` — legacy aliased constants and superseded/redundant objects.
